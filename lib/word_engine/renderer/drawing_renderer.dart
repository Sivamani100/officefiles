import 'package:flutter/material.dart';

class DrawingRenderer extends StatelessWidget {
  const DrawingRenderer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
